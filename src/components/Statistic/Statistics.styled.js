import { theme } from 'commonStyle/Thema';
import styled from 'styled-components';

export const StatisticItems = styled.p`
  font-family: ${theme.fonts.monospace};
  font-size: ${theme.fontSizes.sm};
  color: ${theme.colors.text};
`;
